<!DOCTYPE html>

<html lang="fr">

    <head>

        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=7">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="Site d'annonces immobilières">
        <meta name="keywords" content="maison appartement immobilier agence en ligne">
        <meta name="author" content="David HURTREL">

        <title>SUBL'IMMO - {% block title %}{% endblock %}</title>

        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
        <link rel="stylesheet" href="{{ asset('css/base.css') }}">

        <link rel="icon" type="image/x-icon" href="{{ asset('img/favicon.ico') }}">

        {% block stylesheets %}{% endblock %}

    </head>
    
    <body>

        {% block body %}{% endblock %}

        <footer class="container-fluid bg-light">
            <div class="row text-center p-5">
                <div class="col-4">
                    <h5>QUELQUES LIENS</h5>
                    <div class="dropdown-divider"></div>
                    <p><a href="{{ path('home') }}">Accueil</a></p>
                    <p><a href="{{ path('maison_index') }}">Maisons</a></p>
                    <p><a href="{{ path('cart_index') }}">Panier</a></p>
                    <p><a href="{{ path('contact') }}">Contact</a></p>
                    <p><a href="{{ path('maison_search') }}">Recherche</a></p>
                    <p><a href="{{ path('visite') }}">Visiter</a></p>
                    <p><a href="{{ path('readme') }}">Readme</a></p>
                    <p><a href="{{ path('admin') }}">Admin</a></p>
                    <p>
                        {% if app.user %}
                            <a href="{{ path('app_logout') }}">Déconnexion</a>
                        {% else %}
                            <a href="{{ path('app_login') }}">Connexion</a>
                        {% endif %}
                    </p>
                </div>
                <div class="col-4 my-auto">
                    <img src="{{ asset('img/logo.png') }}" alt="logo SUBL'IMMO" class="w-100">
                </div>
                <div class="col-4">
                    <h5>COORDONNÉES</h5>
                    <div class="dropdown-divider"></div>
                    <p>{{ contact_company }}</p>
                    <p>{{ contact_address }}</p>
                    <p>{{ contact_zip }} {{ contact_city }}</p>
                    <p>{{ contact_phone }}</p>
                    <p>{{ contact_email }}</p>
                </div>
            </div>
            <div class="row">
                <div class="col-12 text-center" id="copyright">
                    <p>Copyright © 2022 - David HURTREL</p>
                </div>
            </div>
        </footer>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
        <script src="{{ asset('js/script.js') }}"></script>

        {% block javascripts %}{% endblock %}

    </body>

</html>
