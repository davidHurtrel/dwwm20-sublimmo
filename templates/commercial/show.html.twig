{% extends 'base.html.twig' %}

{% block title %}Commercial{% endblock %}

{% block body %}
    <h1>Commercial</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commercial.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ commercial.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commercial_index') }}">back to list</a>

    <a href="{{ path('commercial_edit', {'id': commercial.id}) }}">edit</a>

    {{ include('commercial/_delete_form.html.twig') }}
{% endblock %}
